import 'package:flutter/material.dart';

class stylrd extends StatelessWidget{

 const  stylrd({super.key});

  Widget build (context){
    return Text(
              "hi Moral Values",
              style: TextStyle(color: const Color.fromARGB(255, 185, 30, 30)),
            );
  }
}